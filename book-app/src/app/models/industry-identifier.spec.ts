import { IndustryIdentifier } from './industry-identifier';

describe('IndustryIdentifier', () => {
  it('should create an instance', () => {
    expect(new IndustryIdentifier()).toBeTruthy();
  });
});
